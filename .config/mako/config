# Mako notification daemon configuration

# Timeout in milliseconds (5000 = 5 seconds)
default-timeout=5000

# Critical notifications timeout (0 = never timeout)
[urgency=high]
default-timeout=0
border-color=#dddddd
background-color=#191919

# Font
font=monospace 11

# Colors (matching noctalia-shell theme)
background-color=#111111
text-color=#828282
border-color=#3c3c3c
border-size=2
border-radius=10

# Progress bar color
progress-color=#aaaaaa

# Position (top-right corner)
anchor=top-right
margin=20

# Dimensions
width=350
height=150
padding=15

# Grouping
group-by=app-name
