#!/bin/bash
# Notify on successful pull/merge (git pull triggers merge or fast-forward)
repo_name=$(basename "$(git rev-parse --show-toplevel 2>/dev/null || pwd)")
branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown")
notify-send "Git Pull" "Pull/merge completed: $repo_name ($branch)" || true
